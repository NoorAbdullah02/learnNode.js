<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/stylesheet/style.css">
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>

</head>

<body>
    <div class="w-full h-full bg-zinc-900 p-10 text-white">
        <div class="nav mb-10">
            <a class="text-blue-500" href="/">Back to h</a>
        </div>
    
            <h1>All Users</h1>
        <div class="users flex gap-5 mt-5 flex-wrap">
            <div class="user-max-w-72 p-4 bg-zinc-800 rounded-lg">
                <% allusers.forEach(function(user){%>
                    <div class="w-full h-52 bg-red-500 overflow-hidden">
                        <img class="w-full h-full object-cover object-cen" src= "<%= user.photo %>" alt="">
                    </div>

                    <h3 class="text-xl tracking-tighter">
                        <%= user.name%>
                    </h3>
                    <h5 class="text-zinic-500">
                        <%= user.email%>
                    </h5>
                    <div class="flex gap-3">
                        <a class="text-zinic-500" href="/edit/<%= user._id %>">Edit User</a>
                        <a class="text-red-200" href="/delete/<%= user._id %>">Delete User</a>
                    </div>
            </div>
            <% }) %>

        </div>
    </div>

    <script src="/javascript/server.js"></script>
</body>

</html>