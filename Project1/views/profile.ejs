<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Profile Page</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="bg-gradient-to-br from-indigo-950 via-slate-950 to-purple-950 min-h-screen relative overflow-x-hidden">
    <!-- Animated Background Elements -->
    <div class="absolute inset-0 overflow-hidden pointer-events-none">
        <div class="absolute top-20 left-10 w-72 h-72 bg-purple-500/10 rounded-full blur-3xl animate-pulse"></div>
        <div class="absolute bottom-20 right-10 w-96 h-96 bg-blue-500/10 rounded-full blur-3xl animate-pulse" style="animation-delay: 1s"></div>
        <div class="absolute top-1/2 left-1/2 w-80 h-80 bg-pink-500/10 rounded-full blur-3xl animate-pulse" style="animation-delay: 2s"></div>
    </div>

    <!-- Header -->
    <header class="bg-white/5 backdrop-blur-2xl border-b border-white/20 sticky top-0 z-50 shadow-lg shadow-black/20">
        <div class="max-w-6xl mx-auto px-6 py-5 flex justify-between items-center">
            <div class="flex items-center gap-4">
                <div class="w-12 h-12 bg-gradient-to-br from-cyan-400 via-blue-500 to-purple-600 rounded-2xl flex items-center justify-center text-white font-bold text-xl shadow-lg shadow-purple-500/30 rotate-3 hover:rotate-0 transition-transform duration-300">
                    N
                </div>
                <span class="text-white font-bold text-xl tracking-wide">Social Feed</span>
            </div>
            <a href="/logout" class="px-8 py-2.5 bg-gradient-to-r from-rose-500 to-pink-600 hover:from-rose-600 hover:to-pink-700 text-white rounded-2xl transition duration-300 font-semibold shadow-lg shadow-rose-500/30 transform hover:scale-105">
                Logout
            </a>
        </div>
    </header>

    <div class="max-w-6xl mx-auto px-6 py-12 relative z-10">
        
        <!-- Welcome Section -->
        <div class="mb-12 text-center">
            <h1 class="text-6xl font-black text-transparent bg-clip-text bg-gradient-to-r from-cyan-400 via-blue-500 to-purple-600 mb-4 drop-shadow-2xl">
                Welcome back, <%= user.name %>!
            </h1>
            <p class="text-gray-300 text-xl font-light">Share your thoughts with the world</p>
        </div>

        <!-- Create Post Card -->
        <div class="bg-white/10 backdrop-blur-2xl rounded-3xl shadow-2xl border border-white/20 p-10 mb-12 hover:border-cyan-400/50 transition-all duration-500 hover:shadow-cyan-500/20 hover:shadow-2xl">
            <h2 class="text-3xl font-bold text-white mb-8 flex items-center gap-4">
                <div class="w-12 h-12 bg-gradient-to-br from-cyan-400 to-blue-600 rounded-2xl flex items-center justify-center shadow-lg shadow-cyan-500/40">
                    <svg class="w-7 h-7 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2.5" d="M11 5H6a2 2 0 00-2 2v11a2 2 0 002 2h11a2 2 0 002-2v-5m-1.414-9.414a2 2 0 112.828 2.828L11.828 15H9v-2.828l8.586-8.586z"></path>
                    </svg>
                </div>
                Create New Post
            </h2>
            <form action="/post" method="post" class="space-y-5">
                <textarea 
                    placeholder="What's on your mind today? Share something amazing..."
                    class="w-full h-36 resize-none bg-white/5 border-2 border-white/20 rounded-2xl p-5 text-white placeholder-gray-400 focus:outline-none focus:ring-4 focus:ring-cyan-500/50 focus:border-cyan-400 transition-all duration-300 text-lg"
                    name="content" 
                    id="">
                </textarea>
                <button 
                    type="submit"
                    class="w-full py-5 bg-gradient-to-r from-cyan-500 via-blue-600 to-purple-600 hover:from-cyan-600 hover:via-blue-700 hover:to-purple-700 text-white font-bold text-lg rounded-2xl shadow-2xl shadow-cyan-500/40 hover:shadow-cyan-500/60 transition-all duration-300 transform hover:scale-[1.02] active:scale-[0.98] flex items-center justify-center gap-3">
                    <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2.5" d="M12 4v16m8-8H4"></path>
                    </svg>
                    Create Post
                </button>
            </form>
        </div>

        <!-- Posts Section -->
        <div class="posts">
            <div class="flex items-center justify-between mb-10">
                <h3 class="text-4xl font-black text-white flex items-center gap-4">
                    <div class="w-14 h-14 bg-gradient-to-br from-purple-400 to-pink-600 rounded-2xl flex items-center justify-center shadow-lg shadow-purple-500/40">
                        <svg class="w-8 h-8 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2.5" d="M19 11H5m14 0a2 2 0 012 2v6a2 2 0 01-2 2H5a2 2 0 01-2-2v-6a2 2 0 012-2m14 0V9a2 2 0 00-2-2M5 11V9a2 2 0 012-2m0 0V5a2 2 0 012-2h6a2 2 0 012 2v2M7 7h10"></path>
                        </svg>
                    </div>
                    Your Posts
                </h3>
                <span class="text-white bg-gradient-to-r from-cyan-500 to-blue-600 px-6 py-3 rounded-2xl text-base font-bold shadow-lg shadow-cyan-500/30"><%= user.posts.length %> posts</span>
            </div>

            <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
                <% user.posts.reverse().forEach(function(post) { %>
                <!-- Post Card -->
                <div class="bg-white/10 backdrop-blur-2xl rounded-3xl shadow-2xl border border-white/20 p-7 hover:border-cyan-400/60 hover:shadow-2xl hover:shadow-cyan-500/30 transition-all duration-500 transform hover:-translate-y-2 hover:scale-105">
                    
                    <!-- Post Header -->
                    <div class="flex items-center gap-4 mb-5">
                        <div class="w-14 h-14 bg-gradient-to-br from-emerald-400 via-cyan-500 to-blue-600 rounded-2xl flex items-center justify-center text-white font-bold text-xl shadow-lg shadow-cyan-500/40">
                            <%= user.name.charAt(0).toUpperCase() %>
                        </div>
                        <div>
                            <h4 class="text-white font-bold text-lg"><%= user.name %></h4>
                            <p class="text-gray-400 text-sm font-medium">Just now</p>
                        </div>
                    </div>

                    <!-- Post Content -->
                    <p class="text-gray-200 leading-relaxed mb-7 text-base">
                        <%= post.content %>
                    </p>

                    <!-- Post Actions -->
                    <div class="flex gap-3 items-center">
                        <div class="flex items-center gap-2">
                            <span class="text-white bg-pink-500/20 px-3 py-1 rounded-full text-sm font-bold"><%= post.likes.length %></span>
                        </div>
                        <a href="/like/<%= post._id %>" class="flex-1 py-3 bg-gradient-to-r from-pink-500 to-rose-600 hover:from-pink-600 hover:to-rose-700 text-white rounded-2xl transition-all duration-300 font-bold text-center flex items-center justify-center gap-2 shadow-lg shadow-pink-500/30 hover:shadow-pink-500/50 transform hover:scale-105">
                            <svg class="w-5 h-5" fill="<%= post.likes.indexOf(user._id) !== -1 ? 'currentColor' : 'none' %>" stroke="currentColor" viewBox="0 0 24 24">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2.5" d="M4.318 6.318a4.5 4.5 0 000 6.364L12 20.364l7.682-7.682a4.5 4.5 0 00-6.364-6.364L12 7.636l-1.318-1.318a4.5 4.5 0 00-6.364 0z"></path>
                            </svg>
                            <%= post.likes.indexOf(user._id) === -1 ? "Like" : "Unlike" %>
                        </a>
                        <a href="/edit/<%= post._id %>" class="flex-1 py-3 bg-gradient-to-r from-cyan-500 to-blue-600 hover:from-cyan-600 hover:to-blue-700 text-white rounded-2xl transition-all duration-300 font-bold text-center flex items-center justify-center gap-2 shadow-lg shadow-cyan-500/30 hover:shadow-cyan-500/50 transform hover:scale-105">
                            <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2.5" d="M11 5H6a2 2 0 00-2 2v11a2 2 0 002 2h11a2 2 0 002-2v-5m-1.414-9.414a2 2 0 112.828 2.828L11.828 15H9v-2.828l8.586-8.586z"></path>
                            </svg>
                            Edit
                        </a>
                    </div>
                </div>
                <% }); %>
            </div>
        </div>
    </div>

</body>

</html>