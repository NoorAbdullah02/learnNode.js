<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Project</title>
    <link rel="stylesheet" href="/stylesheets/style.css">
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>

</head>

<body>


    <div class="main w-full min-h-screen bg-zinc-900">
        <div class="form p-10 w-1/2 mx-auto text-white">
            <form action="/create" method="post">
                <input class="block w-full rounded-md outline-none px-5 py-2 bg-zinc-800" name="title" type="text"
                    placeholder="Title Write Here!">
                <Textarea class="block w-full rounded-md outline-none px-5 py-2 mt-2 resize-none bg-zinc-800"
                    placeholder="Write the details" name="details"></Textarea>
                <input class="mt-2 bg-blue-600 px-5 py-2" type="submit" placeholder="Create Task">
            </form>
        </div>
        <div class="tasks flex gap-5 flex-wrap justify-center">
            <% if(files.length> 0 ){%>
                <% files.forEach(function(val){ %>
                    <div class="task min-w-72 px-3 py-3 rounded-md bg-zinc-800">
                        <h1 class="text-white text-4xl tracking-tighter mt-2 py-3">
                            <%= val %>
                        </h1>
                        <div class="flex w-full justify-between rounded-md">
                            <a class="initial-block text-black-200 mt-2 px-4 py-2 bg-blue-500"
                                href="/files/<%= val %>">Read more</a>

                            <a class="initial-block text-black-200 mt-2 px-4 py-2 bg-blue-500"
                                href="/edit/<%= val %>">Edit</a>

                        </div>
                    </div>
                    <% })%>
                        <%} else { %>
                            <h3 class="text-white bg-black-700 initial-block">No Task's Are now avaiable</h3>
                            <% } %>
        </div>
    </div>

    <script src=/javascript/script.js></script>
</body>

</html>